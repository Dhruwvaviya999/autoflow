<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required so ShowNotificationAction can post notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Device Boot trigger: re-arm monitors and run boot automations. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Bluetooth triggers: receiving ACL connect/disconnect broadcasts and
         reading device names on Android 12+ needs this runtime permission.
         Requested only when the user configures a Bluetooth automation. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />

    <!-- Wi-Fi SSID triggers only: Android hides network names without fine
         location. Requested only when the user filters by a specific SSID;
         "any network" automations work without it. Never used for location. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Lets the app picker list launchable apps as notification sources
         (package visibility on Android 11+). No broad QUERY_ALL_PACKAGES. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".AutoFlowApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoFlow">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AutoFlow"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Stable in-app target for accessibility testing; reachable from
             Settings → Developer tools. Production behavior never depends
             on this screen. -->
        <activity
            android:name=".ui.testlab.AccessibilityTestLabActivity"
            android:exported="false"
            android:label="Accessibility Test Lab"
            android:theme="@style/Theme.AutoFlow" />

        <!-- Bound by the system only after the user enables AutoFlow in
             Accessibility settings; AutoFlow never enables it itself. Used
             exclusively to run user-configured UI automations and the
             user-started element inspector. All processing is on-device. -->
        <service
            android:name=".services.accessibility.AutoFlowAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Handles Cancel/Continue taps on the UI-automation progress and
             confirmation notifications. Not exported: only AutoFlow's own
             PendingIntents can reach it — no remote control surface. -->
        <receiver
            android:name=".services.accessibility.UiAutomationControlReceiver"
            android:exported="false" />

        <!-- Bound by the system only after the user grants notification
             access in Settings; AutoFlow never requests it automatically. -->
        <service
            android:name=".services.notification.AutoFlowNotificationListenerService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- BOOT_COMPLETED and Bluetooth ACL broadcasts are protected system
             broadcasts (only the OS can send them), so exporting is safe.
             Both receivers only enqueue a WorkManager job. -->
        <receiver
            android:name=".services.system.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".services.system.BluetoothEventReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>